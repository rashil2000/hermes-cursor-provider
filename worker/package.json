{
  "name": "@hermes-cursor-provider/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.19.0"
  },
  "dependencies": {
    "cursor-opencode-provider": "0.7.2"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  },
  "scripts": {
    "bundle": "esbuild worker.mjs --bundle --platform=node --format=esm --target=node22 --legal-comments=external --outfile=worker.bundle.mjs"
  }
}
